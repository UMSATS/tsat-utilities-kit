name: Build and Test CAN Wrapper

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  build:
    name: Build Firmware
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        build_type: [test, production]
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
      with:
        submodules: recursive  # For any git submodules
    
    - name: Setup ARM GCC Toolchain
      uses: carlosperate/arm-none-eabi-gcc-action@v1
      with:
        release: '12.2.Rel1'  # Specify GCC version
    
    - name: Verify Toolchain Installation
      run: |
        arm-none-eabi-gcc --version
        arm-none-eabi-objcopy --version
        arm-none-eabi-size --version
    
    - name: Create Build Directory
      run: mkdir -p build
    
    - name: Build Test Version
      if: matrix.build_type == 'test'
      run: |
        arm-none-eabi-gcc \
          -mcpu=cortex-m4 -mthumb -mfpu=fpv4-sp-d16 -mfloat-abi=hard \
          -DUSE_HAL_DRIVER -DSTM32F407xx -DCWM_API_STANDARD -DRUN_TESTS -DDEBUG \
          -Og -g3 -Wall -fdata-sections -ffunction-sections \
          -Iinc -Isrc \
          -IDrivers/STM32F4xx_HAL_Driver/Inc \
          -IDrivers/CMSIS/Device/ST/STM32F4xx/Include \
          -IDrivers/CMSIS/Include \
          -IMiddlewares/Third_Party/FreeRTOS/Source/include \
          -IMiddlewares/Third_Party/FreeRTOS/Source/CMSIS_RTOS_V2 \
          -specs=nano.specs \
          -T STM32F407VGTx_FLASH.ld \
          -Wl,-Map=build/test.map,--cref -Wl,--gc-sections \
          -o build/test.elf \
          Src/can_wrapper/main.c Src/can_wrapper/can_wrapper.c Src/can_wrapper/test_can_wrapper.c \
          Src/can_wrapper/tx_cache.c Src/can_wrapper/can_command_list.c Src/debug/debug_logger.c \
          Src/debug/log_buffer.c \
          -lc -lm -lnosys
    
    - name: Build Production Version  
      if: matrix.build_type == 'production'
      run: |
        arm-none-eabi-gcc \
          -mcpu=cortex-m4 -mthumb -mfpu=fpv4-sp-d16 -mfloat-abi=hard \
          -DUSE_HAL_DRIVER -DSTM32F407xx -DCWM_API_STANDARD -DNDEBUG \
          -O2 -Wall -fdata-sections -ffunction-sections \
          -Iinc -Isrc \
          -IDrivers/STM32F4xx_HAL_Driver/Inc \
          -IDrivers/CMSIS/Device/ST/STM32F4xx/Include \
          -IDrivers/CMSIS/Include \
          -IMiddlewares/Third_Party/FreeRTOS/Source/include \
          -IMiddlewares/Third_Party/FreeRTOS/Source/CMSIS_RTOS_V2 \
          -specs=nano.specs \
          -T STM32F407VGTx_FLASH.ld \
          -Wl,-Map=build/production.map,--cref -Wl,--gc-sections \
          -o build/production.elf \
          Src/can_wrapper/main.c Src/can_wrapper/can_wrapper.c \
          Src/can_wrapper/tx_cache.c Src/can_wrapper/can_command_list.c Src/debug/debug_logger.c \
          Src/debug/log_buffer.c \
          -lc -lm -lnosys
    
    - name: Generate Hex Files
      run: |
        if [ -f build/test.elf ]; then
          arm-none-eabi-objcopy -O ihex build/test.elf build/test.hex
        fi
        if [ -f build/production.elf ]; then
          arm-none-eabi-objcopy -O ihex build/production.elf build/production.hex
        fi
    
    - name: Show Build Size
      run: |
        if [ -f build/test.elf ]; then
          echo "=== Test Build Size ==="
          arm-none-eabi-size build/test.elf
        fi
        if [ -f build/production.elf ]; then
          echo "=== Production Build Size ==="
          arm-none-eabi-size build/production.elf
        fi
    
    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: firmware-${{ matrix.build_type }}
        path: |
          build/*.elf
          build/*.hex
          build/*.map
        retention-days: 30

  static-analysis:
    name: Static Code Analysis
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4
    
    - name: Run Cppcheck
      uses: deep5050/cppcheck-action@main
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        check_dir: 'src'
        exclude_dir: 'Drivers Middlewares'
        output_file: cppcheck-report.txt
        
    - name: Upload Static Analysis Results
      uses: actions/upload-artifact@v4
      with:
        name: static-analysis-report
        path: cppcheck-report.txt

  check-race-conditions:
    name: Check for Race Conditions
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Repository
      uses: actions/checkout@v4

    - name: Check Single Ownership Pattern (Simple)
      run: |
        echo "=== Checking for Race Condition Fixes ==="
        
        CAN_WRAPPER_FILE=$(find . -path "*/can_wrapper.c" -type f | head -1)
        echo "Found $CAN_WRAPPER_FILE"
        
        # Simple check: ensure critical fixes are present
        echo "Checking for key improvements..."
        
        # 1. Check that Cache_Manager_Thread exists
        if grep -q "Cache_Manager_Thread" "$CAN_WRAPPER_FILE"; then
          echo "✓ Cache_Manager_Thread found"
        else
          echo "ERROR: Cache_Manager_Thread not found"
          exit 1
        fi
        
        # 2. Check that ISR uses osMessageQueuePut instead of direct cache access
        isr_section=$(sed -n '/HAL_CAN_RxFifo0MsgPendingCallback/,/^}/p' "$CAN_WRAPPER_FILE")
        if echo "$isr_section" | grep -q "osMessageQueuePut.*cache_command_queue"; then
          echo "✓ ISR uses command queue instead of direct cache access"
        else
          echo "WARNING: ISR might not be using command queue properly"
        fi
        
        # 3. Check for correct bit operations (look for & instead of |)
        if grep -q "rx_behaviour & RX_" "$CAN_WRAPPER_FILE"; then
          echo "✓ Correct bitwise AND operations found"
        else
          echo "WARNING: Correct bit operations not found"
        fi
        
        # 4. Check that problematic patterns are NOT present
        if grep -v '^\s*[/*]' "$CAN_WRAPPER_FILE" | grep -q "rx_behaviour | RX_"; then
          echo "ERROR: Bitwise OR bug still present"
          exit 1
        else
          echo "✓ No bitwise OR bugs found"
        fi
        
        echo "=== Core race condition fixes verified! ==="
